#ifndef Snake_h
#define Snake_h 

using namespace std;

// Verificar vidas -- atributo que guarda a quantidade de vidas restantes
// Status se ela está viva ou morta -- atributo que guarda info se está viva ou morta
// Verificar se ganhou ou perdeu -- método que verifica se cumpriu os requisitos para ganhar o jogo ou não
// Localização da cobra -- atributo que contém a posição da cobra
// Localização de comida -- atributo com a posição da comida


class Snake{

    private:
    public:

};

#endif